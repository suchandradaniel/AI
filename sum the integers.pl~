% Base case: sum of numbers from 1 to 1 is 1
sum_to_n(1, 1).

% Recursive case: sum to N = N + sum to (N-1)
sum_to_n(N, Sum) :-
    N > 1,
    N1 is N - 1,
    sum_to_n(N1, Sum1),
    Sum is Sum1 + N.
